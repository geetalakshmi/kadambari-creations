# Simple shared admin token (set this in production)
ADMIN_TOKEN=changeme-supersecret
# Where to store uploaded images
UPLOAD_DIR=app/static/uploads
# Allowed origins for CORS (comma-separated). Example: http://localhost:5173,https://your-domain
CORS_ALLOW_ORIGINS=http://localhost:5173
